# Future Part 2 SQLite CRUD Prompt

Use this prompt only after Part 2 is explicitly authorized. Extend the completed Vue + FastAPI CSV-search application with SQLite-backed CRUD that the assignment requires. Preserve the existing CSV search behavior unless the assignment directs otherwise. Add only approved Part 2 data models, endpoints, UI changes, and verification. Do not add authentication, payment processing, or external booking APIs unless the assignment explicitly requires them. Update documentation and evidence with only work that was actually implemented and tested.
